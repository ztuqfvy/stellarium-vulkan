──────── DYN(替身) run 1/3（rc=8）────────
DYNCHECK: 生产者=test(替身场景)
DYNCHECK: 动态帧通路自检：7 判据，2 失败
DYNCHECK: D1-C01 PASS 生产者 failed=0，尾窗[5.3s起]稳态 53.2 fps（下限 36.0）；全程累计 354 帧（含 warmup）
DYNCHECK: D1-C02 FAIL 显示 尾窗稳态 0.0 fps（下限 24.0）；全程推进 73 帧
DYNCHECK: D1-C03 PASS 邮箱 published=354 dropped=0（≤5%）
DYNCHECK: D1-C04 PASS 邮箱帧龄最大 187ms（<500ms；降速段含 ~200ms 属预期）
DYNCHECK: D1-C05 PASS 上传 count=68 mean=0.23ms max=0.86ms（<100ms）
DYNCHECK: D1-C06 PASS 相隔 700ms 两次抓帧不同（内容动态）（1920x1280）
DYNCHECK: D1-C07 FAIL 降速窗内 degraded=false（须 true）/ 恢复后 degraded=false（须 false）
DYNCHECK: VERDICT=FAIL
──────── DYN(替身) run 2/3（rc=8）────────
DYNCHECK: 生产者=test(替身场景)
DYNCHECK: 动态帧通路自检：7 判据，2 失败
DYNCHECK: D1-C01 PASS 生产者 failed=0，尾窗[5.3s起]稳态 53.5 fps（下限 36.0）；全程累计 305 帧（含 warmup）
DYNCHECK: D1-C02 FAIL 显示 尾窗稳态 0.0 fps（下限 24.0）；全程推进 56 帧
DYNCHECK: D1-C03 PASS 邮箱 published=305 dropped=0（≤5%）
DYNCHECK: D1-C04 PASS 邮箱帧龄最大 178ms（<500ms；降速段含 ~200ms 属预期）
DYNCHECK: D1-C05 PASS 上传 count=50 mean=0.26ms max=0.49ms（<100ms）
DYNCHECK: D1-C06 PASS 相隔 700ms 两次抓帧不同（内容动态）（1920x1280）
DYNCHECK: D1-C07 FAIL 降速窗内 degraded=false（须 true）/ 恢复后 degraded=false（须 false）
DYNCHECK: VERDICT=FAIL
──────── DYN(替身) run 3/3（rc=8）────────
DYNCHECK: 生产者=test(替身场景)
DYNCHECK: 动态帧通路自检：7 判据，2 失败
DYNCHECK: D1-C01 PASS 生产者 failed=0，尾窗[5.3s起]稳态 54.0 fps（下限 36.0）；全程累计 324 帧（含 warmup）
DYNCHECK: D1-C02 FAIL 显示 尾窗稳态 0.0 fps（下限 24.0）；全程推进 70 帧
DYNCHECK: D1-C03 PASS 邮箱 published=324 dropped=0（≤5%）
DYNCHECK: D1-C04 PASS 邮箱帧龄最大 184ms（<500ms；降速段含 ~200ms 属预期）
DYNCHECK: D1-C05 PASS 上传 count=65 mean=0.24ms max=0.50ms（<100ms）
DYNCHECK: D1-C06 PASS 相隔 700ms 两次抓帧不同（内容动态）（1920x1280）
DYNCHECK: D1-C07 FAIL 降速窗内 degraded=false（须 true）/ 恢复后 degraded=false（须 false）
DYNCHECK: VERDICT=FAIL
──────── 汇总 ────────
环境：13:43  up 9 days,  2:54, 1 user, load averages: 3.66 3.56 3.90
环境注记：displaysleep=2 分钟（显示器空闲即休眠）
环境注记：Spotlight 批量索引正在运行（mdbulkimport）——帧率读数可能被压低
读数采信规则：D1-C02/D1-C07 量的是「窗口有没有在渲染」——它要求窗口被暴露。
  若同条件的**替身对照**也失败（尤其"全程推进 0 帧"），则该读数反映的是
  「仪器测不到」而不是「被测程序失败」，**不作为退化证据**；
  同时也不改写成 INVALID（原始读数照实保留，只加这条定性）。
DYN 显示/降级判据（替身生产者，不 boot 引擎）：0/3 次 PASS
替身路径不含任何 T18 代码；它失败 ⇒ 与本次改动无关（消费者/环境侧）。

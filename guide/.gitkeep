此目录有意保持为空。

根 CMakeLists.txt 第 203 行会执行：
  CONFIGURE_FILE(... ${PROJECT_SOURCE_DIR}/guide/version.tex @ONLY ...)
输出目标目录必须存在，否则配置失败。

完整guide（含 50MB Images/）只用于生成 guide.pdf，与程序构建无关，
故本仓库不跟踪。若确需生成 PDF，见 tools/setup-upstream-assets.sh。
